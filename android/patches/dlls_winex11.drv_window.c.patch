diff --git a/dlls/winex11.drv/window.c b/dlls/winex11.drv/window.c
index d2d53a40be3..d06daee3f19 100644
--- a/dlls/winex11.drv/window.c
+++ b/dlls/winex11.drv/window.c
@@ -1173,8 +1173,13 @@ static void set_initial_wm_hints( Display *display, Window window )
     /* class hints */
     if ((class_hints = XAllocClassHint()))
     {
+#ifdef __ANDROID__
         class_hints->res_name = process_name;
         class_hints->res_class = process_name;
+#else
+        class_hints->res_name = process_name;
+        class_hints->res_class = process_name;
+#endif
         XSetClassHint( display, window, class_hints );
         XFree( class_hints );
     }
@@ -1182,9 +1187,11 @@ static void set_initial_wm_hints( Display *display, Window window )
     /* set the WM_CLIENT_MACHINE and WM_LOCALE_NAME properties */
     XSetWMProperties(display, window, NULL, NULL, NULL, 0, NULL, NULL, NULL);
     /* set the pid. together, these properties are needed so the window manager can kill us if we freeze */
+#ifndef __ANDROID__
     i = getpid();
     XChangeProperty(display, window, x11drv_atom(_NET_WM_PID),
                     XA_CARDINAL, 32, PropModeReplace, (unsigned char *)&i, 1);
+#endif

     XChangeProperty( display, window, x11drv_atom(XdndAware),
                      XA_ATOM, 32, PropModeReplace, (unsigned char*)&dndVersion, 1 );
@@ -2437,6 +2444,20 @@ static void create_whole_window( struct x11drv_win_data *data )
                                         cx, cy, 0, data->vis.depth, InputOutput,
                                         data->vis.visual, mask, &attr );
     if (!data->whole_window) goto done;
+#ifdef __ANDROID__
+    DWORD pid = 0;
+
+    NtUserGetWindowThread( data->hwnd, &pid );
+
+    XChangeProperty( data->display, data->whole_window, x11drv_atom(_NET_WM_PID),
+                     XA_CARDINAL, 32, PropModeReplace,
+                     (unsigned char *)&pid, 1 );
+
+    UINT_PTR xhwnd = (UINT_PTR)data->hwnd;
+    XChangeProperty( data->display, data->whole_window, x11drv_atom(_NET_WM_HWND),
+                     XA_CARDINAL, 32, PropModeReplace,
+                     (unsigned char *)&xhwnd, sizeof(xhwnd)/4 );
+#endif
     SetRect( &data->current_state.rect, pos.x, pos.y, pos.x + cx, pos.y + cy );
     data->pending_state.rect = data->current_state.rect;
     data->desired_state.rect = data->current_state.rect;
